# HO-001：M0~M6 全部交付，项目进入使用与迭代期

- 日期：2026-08-29
- 交出方：本会话（ZCode）
- 接入方：下一个代理会话 / 用户本人
- Suggested playbooks：`init-project.md`（新会话首步）+ `diagnose.md`（遇 bug 时）

## 本轮（M0 起全程）完成事项

里程碑全绿：M0 环境+219 万行证券库 → M1 LLM 客户端+五件套工具 → M2 ReAct 引擎+四层防护 → M3 路由+会话+审计 → M4 记忆三层 → M5 评测体系 → M6 Web+架构图。测试 **97 passed**；文档体系 ADR-001~007 / CHG-001~009 / PRD / design / 调研×2 / 主索引全部登记；codegraph 索引常青。

## 变更文件与验证状态

- 代码：`src/t2s/`（models/llm/tools/storage/manager/executor/router/ui + config）· `tests/`（97 用例）· `eval/`（golden 50 + runner）· `scripts/`（smoke/seed）· `db/`（schema/indexes/seed）
- 验证：pytest 全绿；金标 50/50 可执行；真机单轮 39.4s 闭环；archify showcase 门禁通过
- git：仓库尚未 `git init`（用户未要求）；如需版本化先 init 再首提

## 当前有效数字（面试可直接引用）

- 评测基线：**78%**（golden 50，结果集比对 v2，glm-4.7）；记忆开/关 **Δ≈0**（样例库仅 2 条，属预期）；语法有效率 96%；平均步数 8.3 / 10,061 token
- 完整报告链：CHG-008 + `eval/reports/SUMMARY.md`

## 下一步（按优先级）

1. **真实使用积累样例库**：Web/REPL 用起来，双通过自动沉淀 ≥20 条后重测消融（命令：`PYTHONPATH=src python -m eval.runner` vs `--no-memory --tag ablation`）
2. 口径语义错误对策实验（J09/J12 类，见 CHG-008 §五）
3. 二期功能：快路径强制计划模式、Web 两段式澄清（PRD 未决 #1）、per-user 权限、MySQL 方言验证
4. 面试准备：按 design.md §11 快答 + CHG-008 三句话结论演练

## 阻塞与未决

- PRD §11 四条 Open Questions 状态不变（Web 澄清形态已裁剪为降级版，见 CHG-009）
- 无阻塞缺陷；`glm-4.7-flash` 免费档晚高峰不可用（用付费档或错峰，见 CHG-007）

## 环境备忘

conda `t2s`（3.11.16）；`.env` 已配智谱 glm-4.7 + embedding-3（key 在位）；codegraph 已索引（AGENTS.md §0 约定优先使用）
