# 本机模拟内网向量服务：Milvus standalone（单机三容器官方形态）
# 前提（二选一）：
#   A. Docker Desktop（WSL2 后端）
#   B. WSL2 发行版内安装 docker-ce（免 Docker Desktop）：wsl --install -d Ubuntu 后
#      在 Ubuntu 内 sudo apt install docker-ce，compose 文件拷入 WSL 执行
# 用法：docker compose -f deploy/docker-compose-milvus.yml up -d
# 项目侧：.env 设 T2S_MILVUS_URI=http://127.0.0.1:19530 即启用 MilvusVectorStore
# 国内拉镜像慢：为 docker daemon 配置镜像加速器，或替换 image 为国内镜像源地址
services:
  etcd:
    image: quay.io/coreos/etcd:v3.5.14
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
    command: etcd -advertise-client-urls=http://etcd:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    volumes:
      - etcd_data:/etcd

  minio:
    image: minio/minio:RELEASE.2024-05-28T17-19-04Z
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    command: minio server /minio_data
    volumes:
      - minio_data:/minio_data

  milvus:
    image: milvusdb/milvus:v2.4.15
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
      COMMON_STORAGETYPE: local
    ports:
      - "19530:19530"   # gRPC（pymilvus 连接端口）
      - "9091:9091"     # 健康检查
    depends_on:
      - etcd
      - minio
    volumes:
      - milvus_data:/var/lib/milvus

volumes:
  etcd_data:
  minio_data:
  milvus_data:
